# Angular Library

This is an Angular library project.

## Prerequisites

- Node.js (v14 or higher recommended)
- npm (v6 or higher)

## Install Dependencies

Install all required dependencies:

```bash
npm install
```

## Build

Build the library:

```bash
npm run build
```

The built library will be located in the `dist/` directory.